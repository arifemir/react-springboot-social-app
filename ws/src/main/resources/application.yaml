server:
  port: 9999
spring:
  datasource:
    url: jdbc:h2:./devdb
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      javax:
        persistence:
          validation:
            mode: none